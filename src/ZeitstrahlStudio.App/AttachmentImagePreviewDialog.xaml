<Window x:Class="ZeitstrahlStudio.App.AttachmentImagePreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Bildvorschau"
        Width="900"
        Height="700"
        MinWidth="560"
        MinHeight="420"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/Theme.Light.xaml" />
                <ResourceDictionary Source="Themes/Typography.xaml" />
                <ResourceDictionary Source="Themes/ControlStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border Padding="16,12" Background="{DynamicResource SurfaceAltBrush}">
            <DockPanel>
                <TextBlock Style="{StaticResource HeadingStyle}" Text="{Binding FileName}" />
                <TextBlock Margin="14,3,0,0"
                           Style="{StaticResource BodyStyle}"
                           Foreground="{DynamicResource MutedTextBrush}"
                           Text="{Binding Dimensions}" />
            </DockPanel>
        </Border>
        <ScrollViewer Grid.Row="1"
                      Margin="12"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
            <Image Source="{Binding PreviewImage}"
                   Stretch="Uniform"
                   SnapsToDevicePixels="True" />
        </ScrollViewer>
        <Button Grid.Row="2"
                Width="100"
                Margin="0,0,16,14"
                HorizontalAlignment="Right"
                IsDefault="True"
                IsCancel="True"
                Content="Schließen" />
    </Grid>
</Window>
