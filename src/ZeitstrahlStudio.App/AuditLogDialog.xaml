<Window x:Class="ZeitstrahlStudio.App.AuditLogDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Änderungsprotokoll"
        Width="960"
        Height="560"
        MinWidth="720"
        MinHeight="420"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource DialogBackgroundBrush}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/Typography.xaml" />
                <ResourceDictionary Source="Themes/ControlStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Padding="20,14"
                Background="{DynamicResource HeaderBackgroundBrush}"
                AutomationProperties.Name="Änderungsprotokoll: Beschreibung">
            <StackPanel>
                <TextBlock Foreground="{DynamicResource HeaderForegroundBrush}"
                           FontSize="{StaticResource FontSize2Xl}"
                           FontWeight="SemiBold"
                           Text="Änderungsprotokoll" />
                <TextBlock Margin="0,4,0,0"
                           Foreground="{DynamicResource HeaderMutedBrush}"
                           Text="Lokal in der Projektdatenbank gespeicherte Ereignisoperationen." />
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="20,16">
            <DataGrid x:Name="AuditEntriesGrid"
                      ItemsSource="{Binding Entries}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.VerticalScrollBarVisibility="Auto"
                      AlternatingRowBackground="{DynamicResource SurfaceAltBrush}"
                      AutomationProperties.Name="Protokolleinträge">
                <DataGrid.Columns>
                    <DataGridTextColumn Width="155"
                                        Header="Zeitpunkt (UTC)"
                                        Binding="{Binding TimestampUtc, StringFormat={}{0:dd.MM.yyyy HH:mm:ss}}" />
                    <DataGridTextColumn Width="100" Header="Vorgang" Binding="{Binding Operation}" />
                    <DataGridTextColumn Width="135" Header="Datensatz" Binding="{Binding EntityType}" />
                    <DataGridTextColumn Width="*" Header="Beschreibung" Binding="{Binding Description}">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextTrimming" Value="CharacterEllipsis" />
                                <Setter Property="TextWrapping" Value="NoWrap" />
                                <Setter Property="ToolTip" Value="{Binding Description}" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridCheckBoxColumn Width="75" Header="Erfolg" Binding="{Binding Succeeded}" />
                </DataGrid.Columns>
            </DataGrid>

            <Border x:Name="AuditEmptyState"
                    Padding="24"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Background="{DynamicResource SurfaceAltBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="{DynamicResource CardCornerRadius}">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasEntries}" Value="False">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Foreground="{DynamicResource SecondaryTextBrush}"
                           Text="Für dieses Projekt sind noch keine Änderungen protokolliert."
                           TextAlignment="Center"
                           TextWrapping="Wrap" />
            </Border>
        </Grid>

        <Border Grid.Row="2"
                Padding="20,12"
                Background="{DynamicResource SurfaceAltBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0">
            <Button Style="{StaticResource BaseButtonStyle}"
                    Width="100"
                    HorizontalAlignment="Right"
                    IsDefault="True"
                    IsCancel="True"
                    AutomationProperties.Name="Änderungsprotokoll schließen"
                    Content="Schließen" />
        </Border>
    </Grid>
</Window>
