<Window x:Class="ZeitstrahlStudio.App.AttachmentAnalysisDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dokumentanalyse"
        Width="820"
        Height="650"
        MinWidth="620"
        MinHeight="480"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/Typography.xaml" />
                <ResourceDictionary Source="Themes/ControlStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Style="{StaticResource HeadingStyle}" Text="{Binding FileName}" />
            <TextBlock Margin="0,5,0,2"
                       Style="{StaticResource BodyStyle}"
                       Foreground="{DynamicResource SubtleTextBrush}"
                       Text="{Binding StatusText}" />
            <TextBlock Style="{StaticResource CaptionStyle}" Text="{Binding MediaType}" />
            <TextBlock Margin="0,4,0,0"
                       Foreground="{DynamicResource WarningTextBrush}"
                       FontWeight="SemiBold"
                       Text="{Binding ExtractionMethodText}" />
            <TextBlock Margin="0,6,0,14"
                       Style="{StaticResource SubtitleStyle}"
                       Text="{Binding DocumentTitle}" />
        </StackPanel>

        <TabControl Grid.Row="1">
            <TabItem Header="Extrahierter Text">
                <TextBox Margin="10"
                         Padding="10"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         Text="{Binding ExtractedText, Mode=OneWay}" />
            </TabItem>
            <TabItem Header="Datumsfundstellen">
                <ListBox Margin="10" ItemsSource="{Binding DateSuggestions}" />
            </TabItem>
            <TabItem Header="Metadaten">
                <ListView Margin="10" ItemsSource="{Binding Metadata}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Width="220" Header="Eigenschaft" DisplayMemberBinding="{Binding Key}" />
                            <GridViewColumn Width="500" Header="Wert" DisplayMemberBinding="{Binding Value}" />
                        </GridView>
                    </ListView.View>
                </ListView>
            </TabItem>
        </TabControl>

        <Button Grid.Row="2"
                Width="100"
                Margin="0,16,0,0"
                HorizontalAlignment="Right"
                IsDefault="True"
                IsCancel="True"
                Content="Schließen" />
    </Grid>
</Window>
