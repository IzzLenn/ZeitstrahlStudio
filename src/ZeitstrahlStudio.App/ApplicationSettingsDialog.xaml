<Window x:Class="ZeitstrahlStudio.App.ApplicationSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Einstellungen"
        Width="520"
        Height="330"
        MinWidth="460"
        MinHeight="300"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource DialogBackgroundBrush}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Themes/Typography.xaml" />
                <ResourceDictionary Source="Themes/ControlStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Padding="20,15" Background="{DynamicResource HeaderBackgroundBrush}">
            <StackPanel>
                <TextBlock Foreground="{DynamicResource HeaderForegroundBrush}"
                           FontSize="{StaticResource FontSize2Xl}"
                           FontWeight="SemiBold"
                           Text="Einstellungen" />
                <TextBlock Margin="0,3,0,0"
                           Foreground="{DynamicResource HeaderMutedBrush}"
                           Text="Das Erscheinungsbild gilt projektübergreifend auf diesem Gerät." />
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1" Margin="24,20">
            <TextBlock Foreground="{DynamicResource SecondaryTextBrush}"
                       FontWeight="SemiBold"
                       Text="Farbschema" />
            <ComboBox x:Name="ApplicationThemeBox"
                      Margin="0,6,0,0"
                      AutomationProperties.Name="Globales Farbschema"
                      ItemsSource="{Binding ThemeOptions}"
                      SelectedItem="{Binding SelectedTheme}"
                      DisplayMemberPath="Label" />
            <TextBlock Margin="0,12,0,0"
                       Foreground="{DynamicResource MutedTextBrush}"
                       TextWrapping="Wrap"
                       Text="Die Auswahl bleibt beim Erstellen, Öffnen und Schließen von Projekten erhalten und wird beim nächsten Programmstart wiederhergestellt." />
        </StackPanel>

        <Border Grid.Row="2"
                Padding="16,11"
                Background="{DynamicResource SurfaceAltBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0">
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button MinWidth="105"
                        Margin="0,0,8,0"
                        IsCancel="True"
                        AutomationProperties.Name="Einstellungen abbrechen"
                        Content="Abbrechen" />
                <Button MinWidth="105"
                        IsDefault="True"
                        Click="Confirm_Click"
                        AutomationProperties.Name="Einstellungen übernehmen"
                        Content="Übernehmen" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
